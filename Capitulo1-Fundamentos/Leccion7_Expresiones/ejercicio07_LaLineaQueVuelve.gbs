//Valiéndote de tus nuevos conocimientos sobre expresiones, 
//modificá el procedimiento Linea para que el cabezal 
//quede en el lugar donde empezó.

procedure Linea(direccion, color, longitud) {
  repeat(longitud) {
    Poner(color)
    Mover(direccion)
  }
  MoverN(longitud, opuesto(direccion))
}